'use client'; import {useState} from 'react'; import Shell from '@/components/Shell'; import {PageHero,DoctorCard} from '@/components/UI'; import {doctors,clinics} from '@/lib/data'; import {Search} from 'lucide-react';
export default function Page(){const[q,setQ]=useState('');const list=doctors.filter(d=>(d.name+d.speciality+(clinics.find(c=>c.id===d.clinicId)?.name||'')).toLowerCase().includes(q.toLowerCase()));return <Shell><PageHero eyebrow="FIND DOCTORS" title="Trusted doctors, through trusted clinics." text="Explore expertise, clinic, fee and availability before choosing your care."/><section className="section"><div className="searchBox"><Search/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search doctor, clinic or speciality"/></div><div className="doctorGrid">{list.map(d=><DoctorCard d={d} key={d.id}/>)}</div></section></Shell>}
