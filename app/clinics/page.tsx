'use client'; import {useState} from 'react'; import Shell from '@/components/Shell'; import {PageHero,ClinicCard} from '@/components/UI'; import {clinics} from '@/lib/data'; import {Search} from 'lucide-react';
export default function Page(){const[q,setQ]=useState('');const list=clinics.filter(c=>(c.name+c.area+c.city+c.specialities.join(' ')).toLowerCase().includes(q.toLowerCase()));return <Shell><PageHero eyebrow="FIND A CLINIC" title="Your clinic, closer than ever." text="Search clinics by speciality, treatment or location, then explore doctors and book directly."/><section className="section"><div className="searchBox"><Search/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search doctors, clinics, treatments or location"/></div><div className="clinicGrid">{list.map(c=><ClinicCard c={c} key={c.id}/>)}</div></section></Shell>}
