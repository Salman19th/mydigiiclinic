import Link from 'next/link'; import {ArrowRight,Building2,MapPin,ShieldCheck,Star} from 'lucide-react'; import {clinics} from '@/lib/data';
export function PageHero({eyebrow,title,text}:{eyebrow:string,title:string,text:string}){return <section className="pageHero"><span className="eyebrow">{eyebrow}</span><h1>{title}</h1><p>{text}</p></section>}
export function ClinicCard({c}:{c:any}){return <Link className="clinicCard" href={'/clinic/'+c.id}><div className="clinicVisual"><Building2/><span>{c.specialities[0]}</span></div><div className="clinicBody"><div className="row"><h3>{c.name}</h3><ShieldCheck/></div><p><MapPin/> {c.area}, {c.city}</p><div className="tags">{c.specialities.slice(0,2).map((s:string)=><span key={s}>{s}</span>)}</div><div className="cardFoot"><span><Star fill="currentColor"/> {c.rating}</span><b>Explore clinic <ArrowRight/></b></div></div></Link>}
export function DoctorCard({d}:{d:any}){const c=clinics.find(x=>x.id===d.clinicId)!;return <div className="doctorCard"><div className="avatar">{d.name.split(' ').pop()[0]}</div><div className="grow"><h3>{d.name}</h3><p>{d.speciality} · {d.experience}</p><span>{d.qual}</span><small><Building2/> {c.name}</small></div><div className="docRight"><b><Star fill="currentColor"/> {d.rating}</b><span>₹{d.fee}</span><Link href={'/doctor/'+d.id}>View profile</Link></div></div>}
